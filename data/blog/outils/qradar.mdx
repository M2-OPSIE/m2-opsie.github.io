---
title: QRadar - Plateforme SIEM/SOAR pour centraliser les alertes de sécurité
date: '2026-02-15'
tags: ['securite', 'siem', 'soar', 'soc', 'qradar', 'incident-response']
draft: false
summary: QRadar est une plateforme SIEM puissante qui centralise les événements de sécurité et automatise la réponse aux incidents. Récemment enrichie avec Criminal IP pour une détection AI-powered.
---

Bon, soyons honnête : **QRadar** c'est un peu comme avoir un "dispatcher" dans ton SOC. 
Imagine un truc qui reçoit des milliers d'alertes par jour de tous tes systèmes (firewalls, serveurs, antivirus, etc.) et qui essaie de comprendre ce qui se passe vraiment. 
C'est pas facile, mais c'est hyper important pour ne pas rater une attaque.

## Le truc qu'il faut comprendre

QRadar fait deux choses principales :

- **SIEM (Security Information and Event Management)** : collecte tous les logs, les corrèle, détecte les patterns bizarres
- **SOAR (Security Orchestration Automated Response)** : automatise les réponses (isoler une machine, bloquer une IP, créer un ticket, etc.)

En gros, c'est ton "cerveau" du SOC. Et depuis poco, ils l'ont branchés à **Criminal IP**, une plateforme d'intelligence de menace basée sur l'IA.
Ça veut dire que QRadar reçoit maintenant des infos sur les IPs malveillantes ET la surface d'attaque en temps quasi-réel. 
Pas mal pour détecter plus vite les trucs chelous.

## Pourquoi c'est utile

### Sans QRadar ou un SIEM

Tu analyzes les logs manuellement = c'est l'enfer.
Tu rates des patterns. Les attaquants se marrent.

### Avec QRadar

- Les logs sont centralisés dans une seule place (hallelujah !)
- Les corrélations se font toutes seules
- Les alertes sont triées par priorité (vraiment utile d'avoir moins de faux positifs)
- Les playbooks d'automatisation répondent instantanément
- Tu as un historique et une timeline de tout ce qui se passe

## L'intégration Criminal IP : c'est quoi le truc ?

Avant : QRadar voyait une connexion vers une IP bizarre. "Hmmm, suspicious."

Maintenant avec Criminal IP : QRadar voit la même connexion + obtient immédiatement :
- L'historique de cette IP (c'est une adresse de botnet connu ?)
- La surface d'attaque associée (y a des ports ouverts dangereux ?)
- Les services accessibles (est-ce que c'est une machine compromise ?)
- Les risques détectés par l'IA (score de dangerosité)

**Résultat** : tu réagis plus rapidement, tu bloques l'IP vite, tu évites une infection.

## Fonctionnalités principales

### Collecte centralisée

QRadar reçoit les données de :
- Firewalls (Palo Alto, Fortinet, Checkpoint, etc.)
- Serveurs Windows et Linux
- Antivirus et EDR
- Proxies
- Équipements réseau
- APIs et connecteurs custom

### Corrélation des événements

QRadar dit : "Tiens, un login échoué sur le serveur SQL, puis 5 minutes après une tentative de reconnaissance réseau, puis une exfiltration de données."
C'est pas juste 3 events random, c'est une **attaque en cours**.

### Détection des anomalies

Avec l'IA et l'apprentissage machine, QRadar apprend ce qui est "normal" dans ton réseau.
Quand quelque chose s'écarte du pattern habituel, bing ! Alerte.

### Dashboards et rapports

Tu veux voir :
- Les incidents du jour ?
- Les IPs les plus malveillantes ?
- Les utilisateurs suspects ?
- La posture de sécurité globale ?

C'est customizable et ça update en temps réel.

### Playbooks d'automatisation

Au lieu de faire ça manuellement à chaque fois :
1. Voir l'alerte
2. Vérifier les logs
3. Escalader si confirmé
4. Bloquer l'IP
5. Isoler la machine
6. Creer un ticket incident

Tu définis une règle une fois, et QRadar la réexécute sur chaque incident similaire. Gain de temps énorme.

## Utilisation basique (en CLI ou API)

### Déploiement avec Docker (démo rapide)

```bash
docker run -d -e ACCEPT_EULA=true \
  -p 8080:8080 \
  -p 443:443 \
  --name qradar-ce \
  qradar-community-edition:latest
```

Puis tu accèdes via `https://localhost`

### Connexion à l'API QRadar

QRadar expose une API REST pour :
- Récupérer les incidents
- Créer des offenses
- Modifier les règles
- Récupérer les logs

Exemple simple en curl pour récupérer les offenses (incidents) :

```bash
curl -k -H "SEC: <your-api-token>" \
  https://your-qradar-instance/api/siem/offenses
```

En Python (plus facile) :

```python
import requests
from requests.auth import HTTPBasicAuth

BASE_URL = "https://your-qradar-instance/api/siem"
HEADERS = {"SEC": "your-api-token"}

# Récupérer les offenses
response = requests.get(
    f"{BASE_URL}/offenses",
    headers=HEADERS,
    verify=False
)

incidents = response.json()
for incident in incidents:
    print(f"[{incident['severity']}] {incident['description']}")
```

### Créer un log source (connecteur)

Pour que QRadar reçoive les logs d'une source :

1. Va dans **Admin** → **Log Sources**
2. Clique sur **Add Log Source**
3. Choisis le type (Syslog, Windows Event Log, SNMP, etc.)
4. Configure l'IP/hostname et le port
5. Sélectionne les parsers appropriés
6. Sauvegarde

QRadar commencera à collecter les logs après 5-10 min.

### Créer une règle simple

```
# Exemple : Alerter si 10 tentatives de login échouées en 5 minutes

Rule: Multiple Failed Logins
Source: Windows Event Log (Event ID 4625)
Condition: COUNT > 10 in 5 minutes
Action: Create Offense with severity HIGH
```

Tu peux le faire en UI ou via la CLI avec des scripts.

## Intégration Criminal IP

Pour activer l'enrichissement Criminal IP :

1. Va dans **Admin** → **Data Sources** → **Threat Intelligence**
2. Ajoute Criminal IP comme source
3. Configure ta clé API Criminal IP
4. Active la corrélation automatique

Ensuite, chaque fois qu'une IP suspecte est détectée :
- QRadar fetch les infos Criminal IP
- Enrichit l'incident avec le contexte d'IA
- Augmente le score de sévérité si nécessaire
- Déclenche des playbooks adaptés

## Cas d'usage concrets

### Détection d'une exfiltration

1. Quelqu'un télécharge 50GB vers une IP inconnue
2. QRadar détecte l'anomalie (faux normalement ?!)
3. Criminal IP dit que c'est une adresse C2 known
4. Un playbook se trigger automatiquement :
   - L'IP est bloquée au firewall
   - L'utilisateur est isolé du réseau
   - Un ticket incident est créé
   - L'équipe SOC est alertée
5. Tu as 5 min pour réagir avant que l'attaquant se casse

### Détection d'une compromission interne

1. Un serveur commence à faire des scans de port sur le réseau (c'est pas normal)
2. Il contacte une IP malveillante (Criminal IP le confirme)
3. QRadar corrèle : c'est pas un accident, c'est une attaque
4. Le serveur est isolé automatiquement
5. Un scan forensique est lancé

### Audit de conformité

Tu dois prouver que tu logs tout et que tu réagis aux incidents ?
QRadar génère des rapports :
- Tous les events sont loggés
- Les alertes sont tracées
- Les réponses automatisées sont documentées
- Parfait pour PCI-DSS, ISO 27001, etc.

## Les limites (soyons honnêtes)

- **Chère** : QRadar coûte cher (compter 50k€+ par an facilement)
- **Complexe** : Faut du temps pour bien la configurer
- **Faux positifs** : Même avec l'IA, tu peux avoir du bruit
- **Dépendance** : Si QRadar tombe, t'es blind sur la sécurité
- **Ingestion de logs** : Faut de la bande passante et du stockage

## Alternatives

Si QRadar c'est trop cher ou trop complex pour toi :

- **Splunk** : Plus flexible, moins "vendeur"
- **ELK Stack** : Open source et gratuit
- **Wazuh** : Vraiment simpa pour les petites structures
- **Elastic Security** : Bon compromise coût/perf
- **Azure Sentinel** : Si t'es dans l'écosystème Microsoft

## Tips pratiques du SOC

1. **Commencer petit** : Active pas toutes les règles d'un coup, tu vas être submergé
2. **Tuner les seuils** : Ajuste les seuils pour réduire les faux positifs
3. **Documenter tes playbooks** : Sinon après t'oublies pourquoi tu as fait tel truc
4. **Utiliser l'enrichissement Criminal IP** : C'est la mise à jour qui change vraiment le game
5. **Faire des tests reguliers** : Lance des tests d'attaque pour valider que tout marche
6. **Former l'équipe** : Un SIEM utilisé par des gens qui comprennent rien, c'est inutile

## Conclusion

QRadar est une bête de guerre dans le monde SIEM/SOAR. 
Pas la meilleure pour tous, mais si tu dois traiter des milliers d'events par seconde et que tu veux automatiser ta réponse aux incidents, c'est un choix solide.
L'intégration Criminal IP est la cerise sur le gâteau pour détecter les menaces plus vite.

Et voilà, tu as maintenant un SOC moins chaotique. Enjoy ! 🔒

---

**Sources utiles** :
- [Doc officielle QRadar](https://www.ibm.com/docs/en/qradar-on-cloud)
- [API QRadar](https://www.ibm.com/docs/en/qradar-on-cloud?topic=apis-qradar-rest-api)
- [Criminal IP Platform](https://www.criminalip.io/)
